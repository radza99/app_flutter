name: app
description: "Smart Hydroponic Flutter App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Internet API
  http: ^1.2.0

  # Graph Chart
  fl_chart: ^0.66.0

  # Firebase
  firebase_core: ^2.32.0
  firebase_database: ^10.5.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true